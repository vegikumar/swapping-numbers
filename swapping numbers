#include<stdio.h>
int main()
{
	int a, b;
	printf("Enter the two values to swap :");
	scanf("%d%d",&a,&b);
	printf("Values before swapping a = %d , b = %d\n",a,b);
	
	a = a+b;
	b = a-b;
	a = a-b;
	
	printf("Values after sawpping a = %d , b = %d",a,b);
	return 0;
}
